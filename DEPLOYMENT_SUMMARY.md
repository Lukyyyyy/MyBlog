# MyBlog 部署工作阶段总结

本文档记录 MyBlog 项目截至当前阶段已经完成的部署工作、采用的技术方案和运行状态，便于后续开发、运维及技术交接。文档不包含生产密码、访问令牌、私钥等敏感信息。

## 1. 部署方案确认

项目采用“本地开发、GitHub 托管、服务器拉取并部署”的工作流：

1. 在本地完成开发和测试。
2. 将应用代码提交到公开仓库 `Lukyyyyy/MyBlog`。
3. 将服务器公共基础设施配置放在私有仓库 `Lukyyyyy/serve-infra`。
4. 服务器通过只读权限从 GitHub 拉取代码，避免生产服务器反向修改远端仓库。
5. 应用更新不自动触发，由维护者确认后手动部署。

服务器同时按可继续部署其他项目的方向规划：应用、数据库和公共入口分离，Caddy 作为共享反向代理，后续项目可以继续接入同一套入口基础设施。

## 2. 应用技术栈

MyBlog 是一个全栈内容管理与博客项目，主要技术包括：

- Next.js：网站前端与服务端渲染。
- React、TypeScript：页面和组件开发。
- Payload CMS：后台管理、内容模型和管理接口。
- MongoDB：Payload CMS 的生产数据库。
- pnpm：Node.js 依赖和构建管理。
- Docker、Docker Compose：生产构建、进程隔离和服务编排。
- Caddy：公网入口、反向代理、HTTPS 管理和域名跳转。
- systemd timer：定时执行备份任务。

## 3. 生产容器化

项目已经完成生产 Docker 化，并在服务器上运行以下核心容器：

- `myblog-app-1`：Next.js 与 Payload CMS 应用。
- `myblog-mongo-1`：MongoDB 数据库。
- `server-infra-caddy-1`：服务器公共 Caddy 入口。

应用镜像使用多阶段构建，构建依赖与最终运行环境分离，以减少生产镜像中的冗余内容。Docker Compose 负责环境变量、持久化目录、健康检查、重启策略和容器网络。

应用端口 `3000` 和 MongoDB 端口 `27017` 只绑定到服务器本机 `127.0.0.1`，不直接暴露到公网。公网请求统一从 Caddy 的 `80/443` 端口进入。

已为容器设置基础资源限制：

- 应用容器内存上限为 1.5 GB。
- MongoDB 容器内存上限为 2 GB。

最终验收时，应用和 MongoDB 容器均处于健康状态，应用本机 HTTP 检查返回 `200`。

## 4. 生产配置与数据持久化

生产环境配置与代码仓库分离：真实生产环境变量不纳入 Git，只提交可供参考的 `.env.production.example`。

生产配置覆盖以下内容：

- Payload CMS 运行密钥。
- MongoDB 认证和连接信息。
- 应用公开地址。
- 生产运行模式及相关参数。

MongoDB 数据和应用上传媒体使用持久化存储，重新创建容器不会直接清除业务数据。生产密钥已经另行备份，Git 提交前也执行了敏感信息扫描，未发现私钥、Token 或真实生产密码进入暂存内容。

## 5. 管理后台初始化

Payload CMS 管理后台已经完成首次初始化，并创建了项目管理员账号：

- 管理员邮箱：`765754281@qq.com`

管理员创建流程已经结束，因此生产站点不会再对任意访问者开放首次管理员注册入口。后续账号和密码维护通过 Payload CMS 管理后台完成。

## 6. 服务器网络与安全

服务器系统为 Ubuntu 24.04，已经完成以下基础安全配置：

- 启用 UFW 防火墙。
- 仅开放 SSH `22`、HTTP `80` 和 HTTPS `443`。
- 数据库和应用内部端口不对公网开放。
- 启用系统安全更新机制。
- 自动更新不自动重启服务器，避免未确认的生产中断。
- GitHub 部署密钥采用单仓库、只读权限。

腾讯云安全组已经确认放行部署所需的公网端口。服务器本机检查显示 Caddy 正在监听 IPv4 和 IPv6 的 `80/443` 端口。

## 7. Caddy 反向代理

公共反向代理配置存放在 `/srv/server-infra`，由独立 Docker Compose 项目运行。当前配置包含：

- `lukybetter.com` 作为主域名。
- `www.lukybetter.com` 永久重定向到主域名。
- 将网站请求反向代理到 MyBlog 应用容器的 `3000` 端口。
- 配置 HSTS 等基础安全响应头。
- 限制使用 HTTP/1.1 和 HTTP/2。
- Caddy 管理接口不发布到宿主机公网。

Caddyfile 已通过配置校验，本机 HTTP 请求能够正常进入 Caddy，并跳转到 HTTPS。

域名证书签发排查也已完成：证书机构访问域名时被腾讯云未备案拦截页接管，因此现阶段无法完成公网证书校验。该结果证明应用、Caddy 和本机监听链路正常，当前公网阻塞点位于域名备案状态。

## 8. 本地自动备份

服务器已部署 MyBlog 自动备份脚本，并通过 systemd service 与 timer 调度：

- 每天凌晨 `03:30` 执行。
- 采用在线备份方式，不要求为了备份主动停止网站。
- 本地备份保留 7 天。
- 备份目录位于 `/var/backups/myblog`。

首次备份已经成功执行并生成备份目录，定时器也已经启用。该方案先保障服务器本地可恢复能力，后续可在此基础上增加异地对象存储备份。

## 9. Git 仓库与版本基线

### MyBlog 应用仓库

本地项目已经安全接入 GitHub 上原有的 `main` 历史，没有覆盖或重建已有提交。部署与页面改动经过格式检查和敏感信息检查后，形成以下本地版本基线：

- 提交：`5ee1ae6 feat: prepare production blog deployment`
- 标签：`v1.0.0`

### serve-infra 基础设施仓库

服务器公共基础设施已经整理为独立仓库，主要包含：

- Caddyfile。
- Caddy Docker Compose 配置。
- MyBlog 备份与恢复脚本。
- systemd service 和 timer 文件。
- 运维说明及对象存储配置示例。

已形成以下本地版本基线：

- 提交：`9ab5640 chore: add shared server infrastructure`
- 标签：`v1.0.0`

服务器生成的部署密钥已经添加到私有仓库并验证成功。读取仓库正常，写入操作被 GitHub 拒绝，说明只读权限按预期生效。

## 10. 当前运行结果

截至本阶段结束，服务器端运行结果如下：

- MyBlog 应用容器正常且健康。
- MongoDB 容器正常且健康。
- Caddy 容器正常运行。
- 应用本机访问返回 HTTP `200`。
- 管理员账号已完成初始化。
- 本地自动备份已启用并完成首次验证。
- 防火墙和容器端口隔离已经生效。
- 应用与基础设施均已建立可追踪的 Git 版本基线。

## 下一步

1. 使用 GitHub 账户写入凭据，将 MyBlog 和 serve-infra 的本地提交及 `v1.0.0` 标签推送到对应远端仓库。
2. 等待 `lukybetter.com` 完成备案并解除腾讯云访问阻断。
3. 备案生效后复验 DNS、Caddy 自动证书签发、主域名访问和 `www` 跳转。
4. 公网访问稳定后，再逐步补充异地对象存储备份、恢复演练和运行监控。
